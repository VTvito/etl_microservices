apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: etl-containers-shared-data
  name: etl-containers-shared-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi